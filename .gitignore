# Rust
rust-core/target/

# WASM build output (generated by scripts/build-wasm.sh)
web/wasm/

# Node / TS
web/node_modules/
web/dist/

# npm pack output (build artifact; published to npm, not committed)
*.tgz

# DeepFilterNet model weights (download separately; do not commit large blobs)
*.onnx
*.tar.gz
models/
