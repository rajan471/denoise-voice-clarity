{
  "name": "denoise-voice-clarity-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "cd .. && npm run build",
    "dev": "vite",
    "prebuild": "cd .. && npm run build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
